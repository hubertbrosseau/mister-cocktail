<h1>List of cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
  <li><%= link_to cocktail.name, cocktail_path(cocktail) %>   <%= link_to "(x)", cocktail, method: :delete %></li>
  <% end %>
</ul>

<%= link_to "Add a new cocktail", new_cocktail_path, class: 'btn btn-success' %>